{
  "name": "my-pnpm-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages-npm/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r --if-present run test"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.18"
  }
}